spring:
  datasource:
    url: jdbc:mysql://localhost:3307/bookstore?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: bookstore
    password: bookstore123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
