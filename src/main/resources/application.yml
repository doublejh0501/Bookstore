spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookinfo?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: spring_user
    password: spring_pw
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080